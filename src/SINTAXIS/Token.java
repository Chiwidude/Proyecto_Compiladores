package SINTAXIS;

public enum Token {

        Reservadas,
        Igual,
        Suma,
        Resta,
        CADENA,
        CONSTANTE_ENTERA,
        CONSTANTE_DECIMAL,
        CONSTANTE_BOOLEANA,
        ERROR
}
